# Runtime

Agent runtime and Web UI used by **智能助理**.

Start from the repository root:

```sh
./run-web.sh
```

The personal-assistant overlay lives in `extensions/personal-assistant/`.
Product documentation is in the [root README](../README.md).
